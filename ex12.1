def sum_of_list(values):
    total = 0
    for val in values:
        try:
            numeric_val = float(val)
        except ValueError as e:
            continue
        total += numeric_val
    return total
